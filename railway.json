{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd render-server && npm install"
  },
  "deploy": {
    "startCommand": "node render-server/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
